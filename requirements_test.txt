# Test dependencies. Install with: pip install -r requirements_test.txt
#
# e3dc-modbus is installed editable from a sibling directory for local dev (live
# changes without reinstalling), even though it's published on PyPI. Clone it next
# to this repo:
#   git clone https://github.com/KaaNee/e3dc-modbus.git ../e3dc-modbus
-e ../e3dc-modbus

homeassistant==2026.9.1
pytest-homeassistant-custom-component
pymodbus>=3.7  # homeassistant.components.modbus's own dependency; needed at import time
modbus-connection[tmodbus]  # homeassistant.components.modbus imports modbus_connection.tmodbus
                             # unconditionally, which needs the "tmodbus" extra (pulls in serialx)
